# Frontend

## Setup
1. Install dependencies:
   - `npm install`
2. Copy `.env.example` to `.env` if you want to override the API URL.

## Run
```bash
npm run dev
```

The app runs on `http://localhost:5173` by default and expects the backend on `http://localhost:8000`.
