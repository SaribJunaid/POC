export default function LoadingScreen() {
  return (
    <div className="card">
      <h2>Loading GHL context…</h2>
      <p>Detecting the current location and user context from the browser.</p>
    </div>
  );
}
